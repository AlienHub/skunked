# 产品需求文档 (PRD) - SKUNKED

| **文档版本** | **V0.0.1**                                                    |
| ------------ | ------------------------------------------------------------- |
| **产品名称** | SKUNKED                                                       |
| **产品类型** | 浏览器安全扩展 (Chrome/Edge Extension)                        |
| **核心定位** | 基于AI语义分析的主动防御型反钓鱼工具，专注对抗针对性SEO投毒。 |
| **目标用户** | 企业财务、行政、HR等高危岗位人员；中小企业安全管理员。        |

## 1. 背景与目标 (Background & Goals)

### 1.1 现状痛点

“银狐”等黑产团伙通过购买搜索引擎关键词（SEO投毒），伪造飞书、钉钉、WPS、QQ等常用办公软件的下载页面。这些页面UI与官网一模一样，且域名不断更换，传统基于黑名单的防护往往滞后，导致用户下载带毒安装包，进而导致企业内网沦陷。

### 1.2 产品目标

- **拦截率**：对针对性高仿钓鱼网站的识别率达到 95% 以上。
- **低打扰**：通过分层过滤机制，确保正常访问官网时 0 延迟，非目标场景不触发 AI 分析。
- **闭环引导**：拦截不是终点，引导用户去**真官网**下载才是安全闭环。

## 2. 核心业务流程 (User Flow)

### 2.1 触发流程

用户在 百度/谷歌/360/必应 搜索 "飞书下载" -> 点击搜索结果 -> 插件介入。

### 2.2 判别与决策逻辑 (核心引擎)

这是本产品的核心壁垒，采用 **漏斗式三层过滤架构**：

1. **第一层：本地极速匹配 (Local Match)**
   - **白名单校验**：如果当前 URL 匹配内置的《官方正版软件库》（如 `feishu.cn`），直接放行，标记为“官方认证”。
   - **黑名单校验**：如果当前 URL 在《已知钓鱼库》中，立即阻断。
   - _耗时：<10ms_
2. **第二层：特征工程与启发式分析 (Heuristics)**
   - **场景触发**：仅当页面 Title 或 Meta 包含敏感词（如“飞书”、“下载”、“钉钉”）且不在白名单时触发。
   - **域名形似度分析**：检测 Typosquatting（如 `fe1shu.cn`, `wps-vip.com`）。
   - _耗时：<50ms_
3. **第三层：AI 智能研判 (AI Analysis)**
   - **触发条件**：通过了前两层，但页面内容高度可疑（如包含下载按钮、页面结构酷似官网）。
   - **执行方式**：异步读取 DOM 结构（提取 Title, H1, Footer, Button Text），发送至云端/本地小模型进行研判。
   - _耗时：500ms - 2s (异步进行，不卡顿页面)_

## 3. 功能需求详述 (Functional Requirements)

### 3.1 官方正版软件库 (Official Software Registry)

功能描述：内置高频被仿冒软件的标准信息，作为真理来源。

数据结构：

- 软件名：飞书 (Feishu)
- 核心关键词：`协同`、`办公`、`下载`
- **官方域名列表**：`['feishu.cn', 'larksuite.com']`
- 官方下载页特征：(用于辅助AI验证)

### 3.2 搜索引擎监听模块 (Search Engine Monitor)

功能描述：在用户使用主流搜索引擎时，预判风险。

逻辑：

1. 监听 DOM 中的搜索框内容或 URL 参数（如 `wd=飞书下载`）。
2. 如果搜索词命中《软件库》，插件进入“高度戒备模式”。
3. 在此模式下打开的任何非白名单链接，自动触发 **AI 研判**。

### 3.3 DOM 提取与 AI 分析引擎 (AI Analysis Engine)

功能描述：通过 LLM 识别“李鬼”。

输入数据 (Prompt Context)：

- **当前 URL**: `www.feishu-down.xyz` (示例)
- **页面标题**: "飞书 - 先进企业协作平台 - 官方下载"
- **核心文本**: "立即下载", "版权所有 2024"
- **目标软件假设**: "飞书" (来自搜索词或页面推断)
- **真官网**: `feishu.cn`

**AI 提示词逻辑 (System Prompt)**：

> "你是一个安全专家。用户正在尝试下载软件【{Software_Name}】。
>
> 真正的官网是【{Official_URL}】。
>
> 当前访问的 URL 是【{Current_URL}】。
>
> 页面标题是【{Page_Title}】。
>
> 请判断：这是一个仿冒的钓鱼网站吗？
>
> 依据：域名是否官方？是否存在混淆视听的子域名？
>
> 输出 JSON：{ is_phishing: true/false, confidence: 0-100, reason: '域名与官方不符' }"

### 3.4 阻断与引导 UI (Intervention UI)

**交互设计**：

1. **全屏覆盖层 (Red Overlay)**：当 AI 判定为钓鱼网站（置信度>90%）。
   - 文案：“警告！您正在访问【飞书】的伪造网站。”
   - 风险提示：“该网站可能包含‘银狐’病毒，会导致电脑被远程控制。”
   - **核心行动点 (Call to Action)**：展示一个巨大的绿色按钮 —— **“前往飞书官方网站下载 (www.feishu.cn)”**。
   - _允许通过入口_：提供一个不起眼的“我坚持访问”链接（用于防止误报）。
2. **顶部警示条 (Yellow Bar)**：当置信度在 60%-90% 之间。
   - 文案：“安全提示：此网站并非【飞书】官方站点，请谨慎下载文件。”
   - 按钮：“去官网”。

## 4. 非功能性需求 (Non-Functional)

### 4.1 隐私保护 (Privacy First)

- **脱敏处理**：除了命中特定关键词（如“下载”、“登录”）的页面外，**绝不**上传用户浏览的 DOM 或 URL 到云端。
- **本地优先**：白名单和正则表达式匹配必须在本地完成。

### 4.2 性能优化

- **DOM加载**：AI 分析必须是 `Async` 的，不能阻塞页面 DOM 的 `OnLoad` 事件。
- **缓存机制**：对于已分析过的 URL，结果缓存 24 小时，避免重复消耗 Token。

## 5. 增长与运营策略 (Growth & Operations)

_作为增长经理，我们需要让安全产品具备病毒式传播能力。_

### 5.1 社区共防机制 (Crowdsourced Defense)

- **机制**：当 AI 成功拦截并被用户确认（用户点击了“去官网”）后，该钓鱼 URL 自动上传至云端数据库。
- **价值**：所有安装插件的用户将在 5 分钟内同步更新黑名单。
- **口号**：“一人发现，全网免疫”。

### 5.2 企业版“影子IT”发现

- **增长点**：针对企业管理员提供仪表盘。
- **功能**：虽然插件保护个人，但可以汇总数据——“本周您的员工尝试访问了 50 次伪造的 WPS 网站，已成功拦截”。这能极大证明产品的 ROI，促进企业统一部署。

### 5.3 误报反馈奖励

- 如果在插件界面提交误报反馈，并在后台核实，赠送高级权益（如更快的云端分析通道），以此优化模型准确率。

## 6. 开发排期规划 (Roadmap)

### Phase 1: MVP (最小可行性产品)

- **核心**：仅支持 Top 10 银狐高频攻击软件（飞书、钉钉、微信、QQ、WPS、向日葵、ToDesk 等）。
- **技术**：本地白名单 + 简单的域名关键词正则匹配（无 AI 模型）。
- **目的**：验证拦截 UI 的转化率（用户是否真的点了去官网）。

### Phase 2: AI 接入 (Current Request)

- **核心**：接入轻量级 LLM (如 Gemini Nano 或云端 API) 进行 DOM 语义分析。
- **场景**：覆盖搜索引擎跳转场景。
- **数据**：建立动态黑名单库。

### Phase 3: 全平台防护

- **扩展**：从浏览器扩展延伸到微信/钉钉内的链接检测（通过小程序或企业微信应用实现）。
