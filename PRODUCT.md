# SKUNKED 产品定义（V1）

本文档是产品与实现对齐的单一来源，优先级高于旧版 PRD 中已废弃或未实现的描述。

## 1. 产品定位

**SKUNKED 专注防护「高仿办公软件/远控类软件」的钓鱼下载页**（SEO 投毒、假官网、typosquatting 等），不是通用反钓鱼产品。

**非目标（V1）**

- 银行/快递/政务等通用钓鱼
- 搜索引擎页面本身的拦截或「高度戒备」
- 未激活终端调用公有云语义研判
- 微信/钉钉内链检测

## 2. 用户与价值

| 用户       | 价值                                                                     |
| ---------- | ------------------------------------------------------------------------ |
| 个人/员工  | 安装后立即获得免费基础防护，误点假下载页前获得阻断或告警，并引导至真官网 |
| 企业管理员 | 在免费基础防护之上批量部署、下发组织策略、启用云语义研判和事件审计       |
| 数据贡献者 | 维护受保护软件目录与确认钓鱼域名                                         |

## 2.1 产品档位原则

插件本身是可独立使用的反钓鱼防护工具，企业能力只能增强体验，不能成为基础防护的前提。

| 档位       | 定位                       | 默认体验                                                           |
| ---------- | -------------------------- | ------------------------------------------------------------------ |
| 免费基础版 | 面向个人与未绑定企业的员工 | 安装即生效；使用本地规则、公开数据集、相似域名检测和风险页引导     |
| 企业增强版 | 面向组织部署               | 在免费基础防护之上增加云语义研判、组织策略、页面文案识别和事件审计 |

免费版不应让用户感到“未激活所以不可用”。未绑定企业时，界面应表达为“基础防护已开启”，而不是“未激活”。

## 3. 保护范围

- **目录**：`open-data/apps.json` 中的受保护软件及其官方域、关键词。
- **门控**：仅当页面落在目录相关场景内才做深度检测；目录外站点默认不打扰。

## 4. 判定规范

### 4.1 品牌信号模式（`brandSignalMode`）

| 模式           | 适用            | 行为                                                                          |
| -------------- | --------------- | ----------------------------------------------------------------------------- |
| `url_only`     | **默认 / 免费** | 仅用 URL（含 host/path）判断是否在防护范围；**不因页面标题/文案单独触发检测** |
| `page_signals` | **仅企业策略**  | URL + 页面标题等文案均可纳入范围判断                                          |

企业通过在云端下发 `policy.brandSignalMode` 开启，终端 Options 只读展示，员工不可自行开关。

### 4.2 分层结论

| 场景                                                  | 结论                    |
| ----------------------------------------------------- | ----------------------- |
| 官方域 / 搜索引擎                                     | `allow`                 |
| 确认钓鱼域 / 本地黑 / 强 typosquatting / 域名高度相似 | `block`（可继续访问）   |
| 目录内品牌 + 无下载意图（仿冒官网等）                 | **`warn`**，可关闭/继续 |
| 目录内品牌 + 下载意图，需云语义                       | 见 4.3                  |
| 目录外                                                | `allow`（不检测）       |

### 4.3 云语义研判（Layer 3）

| 条件                 | 行为                                                                            |
| -------------------- | ------------------------------------------------------------------------------- |
| 未企业激活           | **不调用** `/v1/analyze`；若启发式认为需云复核 → **`warn`**，理由说明需企业激活 |
| 已激活               | 调用企业 endpoint，返回 `allow/warn/block`                                      |
| 云请求失败（已激活） | 降级为低风险 `allow`（可用性优先，与历史行为一致）                              |

### 4.4 交互语义

- `block`：全屏拦截 +「前往官网」+「继续访问」
- `warn`：顶栏告警，可关闭；非硬阻断

## 5. 产品档位（概要）

| 档位       | 检测                                         | 云语义 | 策略                  |
| ---------- | -------------------------------------------- | ------ | --------------------- |
| 免费基础版 | L1 + L2，`url_only`，公开数据集同步/离线快照 | 无     | 本地默认阈值          |
| 企业增强版 | L1 + L2，可 `page_signals`                   | 有     | 云端 `GET /v1/policy` |

## 6. PRD 差异决议

| PRD 项             | 决议                                         |
| ------------------ | -------------------------------------------- |
| 高仿软件下载页防护 | **In Scope**                                 |
| 搜索引擎高度戒备   | **Rejected**（搜索页放行；防护在落地下载页） |
| 监听搜索框         | **Rejected / 后续另评**                      |
| 社区自动黑名单同步 | **Deferred**                                 |
| 企业 ROI 仪表盘    | **Deferred**（API/导出已有）                 |
| 未激活走公有云     | **Rejected**                                 |

## 7. 验收场景（节选）

1. `feishu.cn` → `allow`
2. 确认钓鱼域 → `block`
3. `fe1shu.cn` + 下载意图 → `block`（本地）
4. 仿冒介绍页、无下载按钮 → `warn`
5. 未激活 + 本应上云 → `warn`，且无 `/v1/analyze` 请求
6. `url_only` + 仅标题含「飞书」、URL 中性 → `allow`（不检测）
7. `page_signals` + 仅标题含品牌 + 下载意图 → 进入云路径（已激活时）

---

实现参考：`src/services/securityEngine.ts`、`src/utils/cache.ts`（`DEFAULT_POLICY`）、`docs/cloud-api.md`。
