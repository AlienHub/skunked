{
  "name": "skunked-open-data-api",
  "version": "1.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "description": "Independent SKUNKED Open Data API on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "format:check": "prettier --check \"**/*.{ts,json,md,toml}\""
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260301.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "wrangler": "^4.8.0"
  }
}
